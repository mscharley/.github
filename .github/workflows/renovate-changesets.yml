on:
  workflow_call:
    secrets:
      token:
        description: GitHub token used to create release PR's
        required: true

jobs:
  update:
    name: Update Renovate PR
    runs-on: ubuntu-latest
    if: contains(github.event.pull_request.labels.*.name, 'dependencies')

    steps:
      - name: Update PR
        uses: mscharley/dependency-changesets-action@abab821b4f3cde5721ec3adfdcd9ec0540fd477a # v1.0.3
        with:
          token: ${{ secrets.token }}
          use-semantic-commits: true
          author-name: Renovate Changesets
          author-email: github+renovate@scharley.me
